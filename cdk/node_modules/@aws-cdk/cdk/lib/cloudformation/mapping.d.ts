import { Construct } from '../core/construct';
import { Referenceable } from './stack-element';
export interface MappingProps {
    mapping?: {
        [k1: string]: {
            [k2: string]: any;
        };
    };
}
/**
 * Represents a CloudFormation mapping.
 */
export declare class Mapping extends Referenceable {
    private mapping;
    constructor(scope: Construct, id: string, props: MappingProps);
    /**
     * Sets a value in the map based on the two keys.
     */
    setValue(key1: string, key2: string, value: any): void;
    /**
     * @returns A reference to a value in the map based on the two keys.
     */
    findInMap(key1: string, key2: string): string;
    toCloudFormation(): object;
}
