import { Test } from 'nodeunit';
declare const _default: {
    parseMethodResourcePath: {
        'fails if path does not start with a /'(test: Test): void;
        'fails if there are less than two components'(test: Test): void;
        'fails if a non-supported http method is used'(test: Test): void;
        'extracts resource path and method correctly'(test: Test): void;
    };
    parseAwsApiCall: {
        'fails if "actionParams" is set but "action" is undefined'(test: Test): void;
        'fails since "action" and "path" are mutually exclusive'(test: Test): void;
        'fails if "path" and "action" are both undefined'(test: Test): void;
        '"path" mode'(test: Test): void;
        '"action" mode with no parameters'(test: Test): void;
        '"action" mode with parameters (url-encoded)'(test: Test): void;
    };
};
export = _default;
