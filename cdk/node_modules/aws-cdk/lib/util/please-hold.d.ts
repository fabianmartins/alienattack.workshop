/**
 * Print a message to the logger in case the operation takes a long time
 */
export declare class PleaseHold {
    private readonly message;
    private readonly timeoutSec;
    private handle?;
    constructor(message: string, timeoutSec?: number);
    start(): void;
    stop(): void;
    private printMessage;
}
